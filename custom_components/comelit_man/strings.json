{
  "entity": {
    "event": {
      "doorbell": {
        "name": "Doorbell"
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Comelit Intercom Options",
        "data": {
          "enable_notifications": "Enable doorbell notifications"
        },
        "data_description": {
          "enable_notifications": "Receive doorbell ring and missed call events from the intercom"
        }
      }
    }
  },
  "config": {
    "step": {
      "user": {
        "title": "Comelit Man",
        "description": "Connect to your Comelit 6701W intercom locally.",
        "data": {
          "name": "Name",
          "host": "Host",
          "port": "ICONA Bridge port",
          "http_port": "HTTP port",
          "token": "Authentication token (32-char hex)",
          "password": "Device password (to auto-extract token)"
        },
        "data_description": {
          "name": "Friendly name for this intercom (leave empty to use the host IP)",
          "host": "IP address of your Comelit device",
          "port": "TCP port for ICONA Bridge protocol (default: 64100)",
          "http_port": "HTTP port for device web interface (default: 8080)",
          "token": "Leave empty to auto-extract from device using password",
          "password": "Only needed if token is not provided"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the device",
      "invalid_auth": "Authentication failed — check token or password",
      "token_extraction_failed": "Could not extract token from device backup",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Device is already configured"
    }
  }
}
